# Drupal editor configuration normalization
# @see http://editorconfig.org/

[*]
end_of_line = lf
indent_size = 4 # Better readability, also the PHP standard
max_line_length = 120

[*.{css,html.twig,yml}]
indent_size = 2
